name: Demo Workflow

on: workflow_dispatch
jobs:
  demo_greet:
    runs-on: ubuntu-latest
    steps:
      - name: Print greet message in console
      - uses: actions/checkout@v2
      - run: make say-hello
